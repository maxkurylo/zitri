import { WrapFileNamePipe } from './wrap-file-name.pipe';

describe('WrapFileNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WrapFileNamePipe();
    expect(pipe).toBeTruthy();
  });
});
