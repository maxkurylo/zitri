import { UserTransferStatePipe } from './user-transfer-state.pipe';

describe('UserTransferStatePipe', () => {
  it('create an instance', () => {
    const pipe = new UserTransferStatePipe();
    expect(pipe).toBeTruthy();
  });
});
